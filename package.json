{
  "name": "aadhaar-pdf-decrypt",
  "version": "1.0.0",
  "description": "Decrypt password-protected Aadhaar PDFs using qpdf",
  "main": "server.js",
  "scripts": {
    "postinstall": "apt-get update -y && apt-get install -y qpdf || true",
    "build": "npm install",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
