{
  "name": "aadhaar-pdf-decrypt",
  "version": "1.0.0",
  "description": "Decrypt password-protected Aadhaar PDFs using qpdf",
  "main": "server.js",
  "scripts": {
    "install-qpdf": "apt-get update && apt-get install -y qpdf || (brew install qpdf 2>/dev/null || echo 'qpdf install attempted')",
    "build": "apt-get update -y && apt-get install -y qpdf",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
